// string number boolean null undefined symbol bigint object(array function)

// 局部
// let id = Symbol("id");
// let id1 = Symbol("id");

// 全局
// 如果有 就读 没有就写
// let id = Symbol.for("idsssss");
// let id1 = Symbol.for("id");

// console.info(Symbol.keyFor(id));

// console.info(id === id1);

// Symbol.hasInstance;
// Symbol.iterator;
// Symbol.toPrimitive;
