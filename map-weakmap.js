// map -> 映射
// new Map();
// map.set(key, value);
// map.get(key);
// map.has(key);
// map.delete(key);
// map.clear();
// map.size;

// map.keys();
// map.values();
// map.entries();
// 不允许重复存在

// weakmap -> 弱映射
// weakmap key 必须是对象 不能是原始值
// new WeakMap();
// weakmap.get(key);
// weakmap.set(key, value);
// weakmap.delete(key);
// weakmap.has(key);
